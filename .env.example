# Copy this file to `.env` and fill in your own values.
# Both keys below are SAFE to expose in the frontend — they're protected by
# Supabase Row Level Security. Never put the service_role / secret key here.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
